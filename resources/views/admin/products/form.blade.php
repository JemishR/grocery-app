@extends('admin.layouts.app')@section('content')<div class="card"><h1>{{ $product->exists?'Edit':'Add' }} Product</h1><form method="post" action="{{ $product->exists?'/admin/products/'.$product->id:'/admin/products' }}">@csrf @if($product->exists) @method('PUT') @endif<label>Category</label><select name="category_id">@foreach($categories as $c)<option value="{{ $c->id }}" @selected(old('category_id',$product->category_id)==$c->id)>{{ $c->name }}</option>@endforeach</select><label>Name</label><input name="name" value="{{ old('name',$product->name) }}" required><label>Description</label><textarea name="description">{{ old('description',$product->description) }}</textarea><label>Price</label><input name="price" value="{{ old('price',$product->price) }}" type="number" step="0.01" required><label>Unit</label><input name="unit" value="{{ old('unit',$product->unit) }}" placeholder="1 kg" required><label>Stock</label><input name="stock" value="{{ old('stock',$product->stock??100) }}" type="number" required><label><input type="checkbox" name="is_active" value="1" @checked(old('is_active',$product->exists?$product->is_active:true))> Active</label><button class="btn">Save</button></form></div>@endsection