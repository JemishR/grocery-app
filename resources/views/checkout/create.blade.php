@extends('layouts.app')@section('content')<h1>Delivery details</h1><p>Delivery is currently available only in Surat. Payment/delivery will be handled by the admin team.</p><div class="card"><form method="post" action="/checkout">@csrf<label>Name</label><input name="customer_name" required value="{{ old('customer_name') }}"><label>Mobile</label><input name="mobile" maxlength="10" inputmode="numeric" required value="{{ old('mobile') }}"><label>Full address</label><textarea name="address" rows="4" required>{{ old('address') }}</textarea><label>City</label><input name="city" value="Surat" readonly><label>Pincode</label><input name="pincode" maxlength="6" inputmode="numeric" required value="{{ old('pincode') }}"><label>Notes (optional)</label><textarea name="notes" rows="3">{{ old('notes') }}</textarea><button class="btn">Place order</button></form></div>@endsection